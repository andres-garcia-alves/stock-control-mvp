export interface IUsuario {
    id: number;
    username: string;
    first_name: string;
    last_name: string;
    email: string;
    // is_active: boolean;
    // date_joined: any;
    // last_login: any;
}
